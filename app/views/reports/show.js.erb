$(".report_modal_window").html("<%= j(render partial: "reports/show_modal", locals: {report:@report}) %>")
$('.report_modal').addClass('report_modal-active');
$('.report_mask').addClass('report_mask-active');
