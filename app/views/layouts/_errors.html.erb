    <div class='col-12'>
      <% if obj.errors.any? %>
        <h6><%= obj.errors.count %>件のエラーが発生しました。</h6>
        <ul>
          <% obj.errors.full_messages.each do|message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      <% end %>
    </div>
